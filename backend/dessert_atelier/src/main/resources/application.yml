spring:
  application:
    name: dessert_atelier
  profiles:
    include: 
      - oauth
      - jwt
      - aws
    active:
      - postgresql-test
  servlet:
    multipart:
      enabled: false
      max-file-size: 10MB
      max-request-size: 10MB

server:
  servlet:
    session:
      tracking-modes: cookie

